export const customModulesGuideContent = `# 🧩 自定义模块

自定义模块允许你扩展WebRPA的功能，创建可复用的模块。

---

## 📋 功能概述

### 什么是自定义模块？

自定义模块是用户创建的可复用工作流片段，可以：
- 📦 封装复杂逻辑
- 🔄 在多个工作流中复用
- 🎨 自定义图标和颜色
- ⚙️ 配置输入输出参数
- 📝 添加说明文档

### 使用场景

\`\`\`
1. 常用操作封装
   - 登录流程
   - 数据清洗
   - 格式转换

2. 业务逻辑复用
   - 订单处理
   - 数据验证
   - 报表生成

3. 团队协作
   - 共享标准流程
   - 统一操作规范
   - 提高开发效率
\`\`\`

---

## 🚀 快速开始

### 创建自定义模块

1. 点击左侧边栏的"自定义模块"
2. 点击"新建模块"按钮
3. 填写模块信息：
   - 模块名称
   - 模块描述
   - 选择图标
   - 设置颜色
4. 添加工作流节点
5. 配置输入输出参数
6. 保存模块

### 基本示例

**场景**：创建一个"登录淘宝"的自定义模块

\`\`\`yaml
模块名称: 登录淘宝
模块描述: 自动登录淘宝账号
图标: 🔐
颜色: #FF6B00

输入参数:
  - username: 用户名
  - password: 密码

输出参数:
  - login_success: 登录是否成功

工作流:
  1. 打开页面: https://login.taobao.com
  2. 输入文本: #username → {username}
  3. 输入文本: #password → {password}
  4. 点击元素: #submit
  5. 等待元素: .user-info
  6. 设置变量: login_success = true
\`\`\`

---

## 🎨 模块配置

### 基本信息

**模块名称**：
- 简洁明了
- 描述功能
- 避免重复

**模块描述**：
- 详细说明功能
- 列出使用场景
- 注明注意事项

**图标选择**：
\`\`\`
常用图标:
🔐 登录相关
📊 数据处理
📧 邮件相关
💰 支付相关
📦 物流相关
🔍 搜索相关
\`\`\`

**颜色设置**：
\`\`\`
建议配色:
#FF6B00 - 橙色（操作类）
#4CAF50 - 绿色（成功类）
#2196F3 - 蓝色（信息类）
#F44336 - 红色（警告类）
#9C27B0 - 紫色（特殊类）
\`\`\`

### 输入参数

定义模块需要的输入数据。

**参数配置**：
\`\`\`yaml
参数名: username
参数类型: 字符串
默认值: ""
是否必填: 是
参数说明: 登录用户名
\`\`\`

**参数类型**：
- 字符串
- 数字
- 布尔值
- 列表
- 字典
- 文件路径

**使用输入参数**：
\`\`\`
在模块内部使用 {参数名} 引用:
输入文本: {username}
条件判断: {enable_cache} == true
\`\`\`

### 输出参数

定义模块执行后返回的数据。

**参数配置**：
\`\`\`yaml
参数名: result_data
参数类型: 字典
参数说明: 处理后的数据
\`\`\`

**设置输出参数**：
\`\`\`
在模块内部使用"设置变量"模块:
设置变量: result_data = {处理结果}
\`\`\`

**使用输出参数**：
\`\`\`
在调用模块后:
打印日志: 结果={result_data}
条件判断: {result_data.success} == true
\`\`\`

---

## 🔧 模块开发

### 工作流设计

**模块内部结构**：
\`\`\`
1. 参数验证
   └─ 检查必填参数
   └─ 验证参数格式

2. 主要逻辑
   └─ 执行核心功能
   └─ 处理异常情况

3. 结果返回
   └─ 设置输出参数
   └─ 记录执行日志
\`\`\`

**示例：数据清洗模块**：
\`\`\`
输入: raw_data (原始数据)
输出: clean_data (清洗后的数据)

流程:
1. 条件判断: {raw_data} 是否为空
   └─ 真 → 设置变量: clean_data = []
   └─ 假 → 继续处理

2. 遍历列表: {raw_data}
   └─ 字符串替换: 去除空格
   └─ 字符串替换: 统一大小写
   └─ 列表操作: 添加到结果列表

3. 设置变量: clean_data = {结果列表}
4. 打印日志: 清洗完成，共 {clean_data.length} 条
\`\`\`

### 错误处理

**添加错误处理**：
\`\`\`
1. 条件判断: 检查关键数据
   └─ 假 → 设置变量: error_message = "数据无效"
   └─ 假 → 跳出循环

2. 最后添加:
   条件判断: {error_message} 不为空
   └─ 真 → 打印日志: 错误={error_message}
\`\`\`

### 日志记录

**添加关键日志**：
\`\`\`
打印日志: [模块名] 开始执行
打印日志: [模块名] 参数: {参数列表}
打印日志: [模块名] 处理中...
打印日志: [模块名] 完成，结果: {结果}
\`\`\`

---

## 📦 模块管理

### 模块列表

显示所有自定义模块：
- 模块名称和图标
- 模块描述
- 创建时间
- 使用次数

### 模块操作

**编辑模块**：
- 修改配置
- 更新工作流
- 调整参数

**复制模块**：
- 快速创建相似模块
- 保留原模块

**导出模块**：
- 导出为JSON文件
- 分享给他人
- 备份模块

**导入模块**：
- 从JSON文件导入
- 批量导入

**删除模块**：
- 永久删除模块
- 不影响已使用的工作流

### 模块分类

**使用标签分类**：
\`\`\`
登录类: 🔐
数据类: 📊
通知类: 📧
支付类: 💰
\`\`\`

---

## 🎯 使用自定义模块

### 添加到工作流

**方法1：从侧边栏拖拽**
1. 在左侧边栏找到自定义模块
2. 拖拽到画布

**方法2：双击画布**
1. 双击画布空白处
2. 在弹出的模块选择器中选择
3. 自定义模块显示在列表中

**方法3：右键菜单**
1. 右键点击画布
2. 选择"添加模块"
3. 选择自定义模块

### 配置模块参数

**输入参数配置**：
\`\`\`
模块: 登录淘宝
参数:
  username: {用户名变量}
  password: {密码变量}
\`\`\`

**使用输出参数**：
\`\`\`
登录淘宝 → 条件判断
条件: {login_success} == true
  └─ 真 → 继续后续操作
  └─ 假 → 发送失败通知
\`\`\`

---

## 💡 最佳实践

### 1. 单一职责

\`\`\`
❌ 不推荐: 一个模块做太多事情
✅ 推荐: 每个模块只做一件事

示例:
- 登录模块：只负责登录
- 数据采集模块：只负责采集
- 数据处理模块：只负责处理
\`\`\`

### 2. 参数化设计

\`\`\`
❌ 不推荐: 硬编码配置
✅ 推荐: 使用参数

示例:
❌ 打开页面: https://example.com
✅ 打开页面: {base_url}
\`\`\`

### 3. 完善的文档

\`\`\`
模块描述应包含:
- 功能说明
- 参数说明
- 使用示例
- 注意事项
\`\`\`

### 4. 错误处理

\`\`\`
必须处理的情况:
- 参数为空
- 网络错误
- 元素找不到
- 数据格式错误
\`\`\`

### 5. 版本管理

\`\`\`
建议:
- 在模块名称中包含版本号
- 重大更新时创建新模块
- 保留旧版本以兼容
\`\`\`

---

## 🔄 模块更新

### 更新策略

**向后兼容**：
\`\`\`
✅ 可以做:
- 添加新参数（带默认值）
- 优化内部逻辑
- 修复bug

❌ 避免做:
- 删除参数
- 修改参数类型
- 改变输出格式
\`\`\`

**版本控制**：
\`\`\`
命名规范:
- 登录淘宝 v1.0
- 登录淘宝 v2.0
- 登录淘宝 v2.1
\`\`\`

---

## 📚 实战案例

### 案例1：通用登录模块

\`\`\`yaml
模块名称: 通用网站登录
图标: 🔐
颜色: #FF6B00

输入参数:
  - login_url: 登录页面URL
  - username_selector: 用户名输入框选择器
  - password_selector: 密码输入框选择器
  - submit_selector: 提交按钮选择器
  - username: 用户名
  - password: 密码

输出参数:
  - login_success: 登录是否成功

工作流:
  1. 打开页面: {login_url}
  2. 输入文本: {username_selector} → {username}
  3. 输入文本: {password_selector} → {password}
  4. 点击元素: {submit_selector}
  5. 等待: 3秒
  6. 设置变量: login_success = true
\`\`\`

### 案例2：数据验证模块

\`\`\`yaml
模块名称: 数据格式验证
图标: ✅
颜色: #4CAF50

输入参数:
  - data: 待验证的数据
  - required_fields: 必填字段列表

输出参数:
  - is_valid: 是否有效
  - error_message: 错误信息

工作流:
  1. 设置变量: is_valid = true
  2. 设置变量: error_message = ""
  3. 遍历列表: {required_fields}
     └─ 条件判断: {data[当前字段]} 为空
        └─ 真 → 设置变量: is_valid = false
        └─ 真 → 设置变量: error_message = "缺少字段: {当前字段}"
        └─ 真 → 跳出循环
\`\`\`

### 案例3：通知发送模块

\`\`\`yaml
模块名称: 多渠道通知
图标: 📢
颜色: #2196F3

输入参数:
  - title: 通知标题
  - content: 通知内容
  - channels: 通知渠道列表 ["email", "wechat", "dingtalk"]

输出参数:
  - send_success: 发送是否成功
  - failed_channels: 失败的渠道

工作流:
  1. 设置变量: failed_channels = []
  2. 遍历列表: {channels}
     └─ 条件判断: {当前渠道} == "email"
        └─ 真 → 发送邮件
     └─ 条件判断: {当前渠道} == "wechat"
        └─ 真 → 企业微信通知
     └─ 条件判断: {当前渠道} == "dingtalk"
        └─ 真 → 钉钉通知
  3. 设置变量: send_success = {failed_channels.length} == 0
\`\`\`

---

## 🐛 常见问题

### 模块找不到

**原因**：
- 模块被删除
- 模块文件损坏

**解决**：
- 重新创建模块
- 从备份恢复

### 参数传递失败

**原因**：
- 参数名称不匹配
- 参数类型错误

**解决**：
- 检查参数名称
- 验证参数类型
- 查看执行日志

### 模块执行失败

**调试方法**：
\`\`\`
1. 在模块内部添加日志
2. 检查输入参数值
3. 单独测试模块
4. 查看错误信息
\`\`\`

---

## 🔗 相关文档

- [变量系统详解](variables-guide) - 参数传递
- [高级功能](advanced-features) - 子流程
- [调试与错误处理](debug-guide) - 模块调试
`
