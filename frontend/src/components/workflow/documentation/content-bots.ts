export const botsGuideContent = `# 💬 QQ 与微信机器人

本章介绍如何使用 WebRPA 实现 QQ 自动化（基于 NapCat 框架）和微信自动化（基于键鼠模拟）。

---

## QQ 机器人（基于 NapCat）

QQ 自动化共 **8 个模块**，通过 NapCat 框架与 QQ 通信，支持发送消息/图片/文件、获取好友/群组信息等。

### 前置配置

1. 启动 WebRPA 自带的 NapCat 服务（工具栏「NapCat」按钮）
2. 用手机或 PC 端 QQ 扫码登录
3. 登录成功后即可使用所有 QQ 模块

> **注意**：QQ 自动化仅供个人非商业使用，遵守 NapCat 非商业使用协议。

---

### 发送 QQ 消息（qq_send_message）

向好友或群组发送文字消息。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 消息类型 | private（私聊）/ group（群聊） | \`private\` |
| 目标 ID | 好友 QQ 号或群号 | \`123456789\` |
| 消息内容 | 要发送的文字（支持变量） | \`任务已完成：{result}\` |
| 结果变量 | 保存发送结果 | \`qq_msg_result\` |

**示例**（工作流完成后通知自己）：
\`\`\`
发送QQ消息 → 类型: private, 目标: {my_qq}, 内容: 任务完成！共采集{count}条数据
\`\`\`

---

### 发送 QQ 图片（qq_send_image）

向好友或群组发送图片。

| 参数 | 说明 |
|------|------|
| 消息类型 | private / group |
| 目标 ID | QQ 号或群号 |
| 图片路径 | 本地图片路径或 URL |
| 结果变量 | 发送结果 |

---

### 发送 QQ 文件（qq_send_file）

向好友或群组发送文件（文档、压缩包等）。

| 参数 | 说明 |
|------|------|
| 消息类型 | private / group |
| 目标 ID | QQ 号或群号 |
| 文件路径 | 本地文件的完整路径 |
| 结果变量 | 发送结果 |

---

### 等待 QQ 消息（qq_wait_message）

等待并接收 QQ 消息，可按来源、内容过滤。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 来源类型 | any（任意）/ private（私聊）/ group（群聊） | \`private\` |
| 来源 ID | 指定 QQ 号或群号（可选） | \`123456789\` |
| 匹配模式 | contains（包含）/ equals（完全匹配）/ regex（正则） | \`contains\` |
| 匹配内容 | 要匹配的文字（可选，为空则接收所有消息） | \`开始\` |
| 等待超时（秒） | 0 表示无限等待 | \`60\` |
| 结果变量 | 保存收到的消息对象 | \`qq_received_message\` |

**结果变量包含**：
- \`sender_id\`：发送者 QQ 号
- \`content\`：消息文字内容
- \`message_type\`：private 或 group
- \`group_id\`：群号（群消息时有值）
- \`time\`：收到时间戳

**示例**（等待指令后执行任务）：
\`\`\`
等待QQ消息 → 来源: 私聊, 匹配: 开始采集 → 结果变量: msg
打印日志 → 收到指令，开始执行...
执行主要任务...
发送QQ消息 → 类型: private, 目标: {msg.sender_id}, 内容: 任务完成！
\`\`\`

---

### 获取好友列表（qq_get_friends）

获取当前 QQ 账号的所有好友信息。

| 参数 | 说明 |
|------|------|
| 结果变量 | 保存好友列表（数组，每项含 qq、nickname 等） |

---

### 获取群列表（qq_get_groups）

获取当前 QQ 账号加入的所有群组。

| 参数 | 说明 |
|------|------|
| 结果变量 | 保存群列表（含 group_id、group_name 等） |

---

### 获取群成员（qq_get_group_members）

获取指定群的所有成员信息。

| 参数 | 说明 |
|------|------|
| 群号 | 目标群的 group_id |
| 结果变量 | 保存成员列表 |

---

### 获取登录信息（qq_get_login_info）

获取当前已登录的 QQ 账号信息（QQ 号、昵称等）。

| 参数 | 说明 |
|------|------|
| 结果变量 | 保存登录信息字典 |

---

## 微信自动化

微信自动化共 **2 个模块**，通过模拟键鼠操作实现，支持 PC 端微信任意版本。

### 使用前提

- PC 端微信已登录且窗口已打开
- 执行期间不要手动操作鼠标键盘
- 微信窗口不能最小化

---

### 微信发送消息（wechat_send_message）

向指定联系人或群组发送文字消息。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 收件人 | 联系人或群组名称（支持模糊匹配） | \`文件传输助手\` |
| 消息内容 | 要发送的文字 | \`Hello {name}！\` |
| 结果变量 | 发送结果 | \`wechat_msg_result\` |

> **提示**：「文件传输助手」是发给自己的最佳选择，用于测试。

---

### 微信发送文件（wechat_send_file）

向联系人或群组发送本地文件。

| 参数 | 说明 |
|------|------|
| 收件人 | 联系人或群组名称 |
| 文件路径 | 本地文件完整路径 |
| 结果变量 | 发送结果 |

---

## 📋 实战示例：群通知机器人

\`\`\`mermaid
flowchart TD
    A[等待QQ消息\n匹配: 查询] --> B[提取查询关键词]
    B --> C[HTTP请求查询API]
    C --> D[处理结果数据]
    D --> E[发送QQ消息\n回复查询结果]
    E --> A
\`\`\`

---

## 💡 使用技巧

- **QQ 号获取**：用「获取登录信息」模块获取自己的 QQ 号，再结合「设置变量」保存
- **群播通知**：先用「获取群列表」获取所有群，再遍历发送
- **消息路由**：用条件判断对不同内容的消息做不同处理，实现关键词回复
- **微信备份**：定期截图或提取网页数据后，通过微信发文件给自己，实现数据备份
`
