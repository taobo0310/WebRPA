export const mathFlowGuideContent = `# 🔢 数学统计与 CSV 处理

---

## ➕ 数学运算

| 模块 | 说明 |
|------|------|
| math_round | 四舍五入（可设小数位数） |
| math_floor | 向下取整 |
| math_modulo | 取余运算 x % y |
| math_abs | 绝对值 |
| math_sqrt | 平方根 |
| math_power | 幂运算 x^y |
| math_log | 对数（支持 2/10/e 底数） |
| math_trig | 三角函数（sin/cos/tan 及反函数） |
| math_exp | 自然指数 e^x |
| math_gcd | 最大公约数 |
| math_lcm | 最小公倍数 |
| math_factorial | 阶乘 n! |
| math_permutation | 排列数 P(n,r) |
| math_percentage | 百分比计算 |
| math_clamp | 限制数值范围 |
| math_base_convert | 进制转换（2/8/10/16进制互转） |
| math_random_advanced | 生成随机数列表 |

所有数学模块的配置都是：**输入数值** → **结果变量**。

**示例**（计算折扣后价格）：
\`\`\`
设置变量 → price = 199
百分比计算 → 数值: {price}, 总数: 100 → 折扣: 80%
幂运算 → 不适用本例...
\ 直接用表达式更简单：设置变量 → discounted = {price} * 0.8
\`\`\`

---

## 📊 统计分析

| 模块 | 说明 |
|------|------|
| stat_median | 中位数（排序后中间值） |
| stat_mode | 众数（出现最多的值） |
| stat_variance | 方差（离散程度） |
| stat_stdev | 标准差（方差的平方根） |
| stat_percentile | 百分位数（如第 75 百分位） |
| stat_normalize | 归一化（映射到 0-1 范围） |
| stat_standardize | 标准化（Z-score，均值为0标准差为1） |

**配置**：

| 参数 | 说明 |
|------|------|
| 数据列表 | 包含数字的列表变量 |
| 百分位（stat_percentile） | 0-100 的数值 |
| 结果变量 | 保存计算结果 |

**示例**（计算学生成绩统计）：
\`\`\`mermaid
flowchart TD
    A[读取Excel成绩表] --> B[提取分数列表]
    B --> C1[计算平均值\nlist_average]
    B --> C2[计算中位数\nstat_median]
    B --> C3[计算标准差\nstat_stdev]
    C1 --> D[汇总输出]
    C2 --> D
    C3 --> D
\`\`\`

---

## 📋 CSV 处理

### CSV 解析（csv_parse）

将 CSV 格式的字符串解析为列表的列表（二维数组）。

| 参数 | 说明 | 示例 |
|------|------|------|
| CSV 内容 | CSV 格式字符串变量 | \`{file_content}\` |
| 分隔符 | 列分隔符（默认逗号） | \`,\` |
| 是否有标题行 | 第一行是否为列名 | 是 |
| 结果变量 | 解析后的数据（列表或字典列表） | \`csv_data\` |

**示例**（读取并处理 CSV 文件）：
\`\`\`
读取文本文件 → 路径: data.csv → 结果变量: raw_csv
CSV解析 → 内容: {raw_csv}, 有标题行: 是 → 结果变量: rows
遍历列表 → 列表: {rows}, 项变量: row
  └── 打印日志 → 名称: {row["name"]}, 年龄: {row["age"]}
\`\`\`

---

### CSV 生成（csv_generate）

将列表数据转换为 CSV 格式字符串，或直接保存为 CSV 文件。

| 参数 | 说明 | 示例 |
|------|------|------|
| 数据 | 列表或字典列表变量 | \`{data_list}\` |
| 分隔符 | 列分隔符 | \`,\` |
| 包含标题 | 是否添加列名行 | 是 |
| 保存路径 | CSV 文件保存路径（可选） | \`C:\\output.csv\` |
| 结果变量 | 保存 CSV 字符串或文件路径 | \`csv_result\` |

---

### 列表转字符串高级版（list_to_string_advanced）

将列表按指定格式连接为字符串，支持复杂分隔符和格式化。

| 参数 | 说明 | 示例 |
|------|------|------|
| 列表变量 | 要连接的列表 | \`{my_list}\` |
| 分隔符 | 连接符 | \`\\n\`（换行）|
| 前缀/后缀 | 每个元素的前后缀 | \`- \` |
| 结果变量 | 保存字符串 | \`formatted_text\` |

---

## 🎲 概率触发器（probability_trigger）

以指定概率决定工作流是否继续执行，用于随机化行为。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 触发概率 | 0-100 的百分比 | \`30\`（30% 概率触发） |

**工作方式**：
- 有**触发**和**跳过**两个输出端口
- 以设定概率走「触发」分支，其余走「跳过」分支

**用途**：
- 随机化操作间隔，模拟真实用户行为
- A/B 测试：随机分流流量
- 随机抽样：30% 概率执行额外操作

---

## 🔄 遍历字典（foreach_dict）

遍历字典的所有键值对，每次迭代获取一个键和对应的值。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 字典变量 | 要遍历的字典 | \`{my_dict}\` |
| 键变量名 | 每次迭代的键名 | \`key\` |
| 值变量名 | 每次迭代的值 | \`value\` |
| 索引变量名 | 迭代次数（从0开始） | \`index\` |

**示例**（处理字典数据）：
\`\`\`
设置变量 → config = {"host": "localhost", "port": "8080", "debug": "true"}
遍历字典 → 字典: {config}, 键: key, 值: value
  └── 打印日志 → 配置项: {key} = {value}
\`\`\`

---

## 💡 使用技巧

- **数学运算组合**：多个数学模块可以串联，第一个的结果变量作为第二个的输入
- **CSV 处理流程**：读取文件 → CSV解析 → 遍历处理 → CSV生成 → 写入文件
- **统计分析**：先用「list_filter」过滤无效数据，再进行统计计算
- **概率触发**：建议在机器人操作中加入5-30%的概率触发随机等待，降低被检测风险
`
