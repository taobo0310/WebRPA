export const desktopGuideContent = `# 🖥️ 桌面应用自动化

本章介绍如何使用 WebRPA 自动化控制 Windows 桌面应用程序（如记事本、Excel、浏览器、自定义软件等），基于 Windows UI Automation 技术实现。

---

## 概述

桌面应用自动化模块共 **26 个**，支持：
- 启动、连接、关闭任意 Windows 桌面程序
- 窗口管理（激活、最大化、移动、缩放）
- 查找并操作 UI 控件（按钮、输入框、下拉框、菜单、列表等）
- 键盘/鼠标与控件交互
- 截取窗口图像

> **前提条件**：操作系统为 Windows 10/11，目标应用无需特殊配置。

---

## 🚀 应用管理

### 启动桌面应用（desktop_app_start）

启动一个桌面程序，并等待其完全加载。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 程序路径 | EXE 文件的完整路径 | \`C:\\Windows\\notepad.exe\` |
| 启动参数 | 命令行参数（可选） | \`/A myfile.txt\` |
| 等待就绪 | 等待程序响应（秒） | \`5\` |
| 结果变量 | 保存进程 ID | \`app_pid\` |

**示例工作流**：

\`\`\`
启动桌面应用 → 程序路径: C:\\Windows\\notepad.exe → 结果变量: notepad_pid
\`\`\`

---

### 连接桌面应用（desktop_app_connect）

连接到**已经运行**的桌面程序（通过程序名或窗口标题查找）。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 连接方式 | 按进程名/窗口标题/PID | 进程名 |
| 进程名 | 不含扩展名的程序名 | \`notepad\` |
| 窗口标题 | 窗口标题（支持模糊匹配） | \`无标题 - 记事本\` |
| 结果变量 | 保存应用句柄 | \`app_handle\` |

---

### 关闭桌面应用（desktop_app_close）

关闭指定的桌面程序。

| 参数 | 说明 |
|------|------|
| 应用变量 | 之前连接/启动返回的变量 |
| 强制关闭 | 开启则强制终止进程 |

---

### 获取应用信息（desktop_app_get_info）

获取应用程序的详细信息。

| 参数 | 说明 |
|------|------|
| 应用变量 | 应用句柄变量 |
| 结果变量 | 保存信息（含 pid、标题、路径等） |

---

### 等待应用就绪（desktop_app_wait_ready）

等待应用程序变为可响应状态（不再转圈），适合启动慢的软件。

| 参数 | 说明 |
|------|------|
| 应用变量 | 应用句柄变量 |
| 超时（秒） | 最长等待时间 |

---

## 🪟 窗口管理

### 激活窗口（desktop_window_activate）

将指定窗口置于前台并获取焦点。

\`\`\`
连接桌面应用 → 应用变量: app
激活窗口 → 应用变量: {app}
\`\`\`

---

### 设置窗口状态（desktop_window_state）

最大化、最小化、还原或隐藏窗口。

| 状态 | 说明 |
|------|------|
| maximize | 最大化 |
| minimize | 最小化 |
| restore | 还原 |
| hide | 隐藏 |
| show | 显示 |

---

### 移动窗口（desktop_window_move）

将窗口移动到屏幕指定位置。

| 参数 | 说明 |
|------|------|
| X 坐标 | 窗口左上角 X |
| Y 坐标 | 窗口左上角 Y |

---

### 调整窗口大小（desktop_window_resize）

| 参数 | 说明 |
|------|------|
| 宽度 | 像素 |
| 高度 | 像素 |

---

### 获取窗口列表（desktop_window_list）

获取当前所有打开窗口的列表，结果为数组，每项含标题、进程名、PID。

| 参数 | 说明 |
|------|------|
| 结果变量 | 保存窗口列表 |

---

### 截取窗口图像（desktop_window_capture）

对指定窗口截图保存为图片文件。

| 参数 | 说明 |
|------|------|
| 应用变量 | 应用句柄 |
| 保存路径 | 图片保存路径 |
| 结果变量 | 保存图片路径 |

---

## 🎛️ 控件操作

### 查找控件（desktop_find_control）

在应用中查找 UI 控件，是所有控件操作的基础。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 应用变量 | 应用句柄 | \`{app}\` |
| 控件类型 | Button/Edit/Text/ComboBox 等 | \`Button\` |
| 控件名称 | 控件的 Name 属性 | \`确定\` |
| 自动化ID | AutomationId（更精确） | \`btnOK\` |
| 类名 | ClassName 属性 | \`Edit\` |
| 超时（秒） | 等待控件出现 | \`10\` |
| 结果变量 | 保存控件引用 | \`ctrl\` |

**如何获取控件信息**：
使用 WebRPA 内置的**桌面元素选择器**（工具栏「桌面选择器」按钮），悬停到控件上即可获取其属性。

---

### 获取控件信息（desktop_control_info）

获取控件的所有属性（类型、名称、矩形、是否可用等）。

| 参数 | 说明 |
|------|------|
| 控件变量 | 查找到的控件 |
| 结果变量 | 保存属性字典 |

---

### 获取控件树（desktop_control_tree）

获取应用的完整控件层级树，用于分析程序结构。

| 参数 | 说明 |
|------|------|
| 应用变量 | 应用句柄 |
| 深度 | 树的最大深度（1-10） |
| 结果变量 | 保存树形结构 |

---

### 等待控件（desktop_wait_control）

等待控件出现后再继续执行，适合加载较慢的界面。

| 参数 | 说明 |
|------|------|
| 应用变量 | 应用句柄 |
| 控件条件 | 同查找控件 |
| 超时（秒） | 最长等待 |
| 结果变量 | 保存控件 |

---

### 点击控件（desktop_click_control）

点击按钮、链接等可点击控件。

| 参数 | 说明 |
|------|------|
| 控件变量 | 目标控件 |
| 点击方式 | 单击/双击/右键 |
| 双击间隔 | 双击时的间隔（毫秒） |

**示例**（点击「确定」按钮）：

\`\`\`
查找控件 → 控件名称: 确定, 控件类型: Button → 结果变量: btn_ok
点击控件 → 控件变量: {btn_ok}
\`\`\`

---

### 输入文本（desktop_input_control）

向文本输入框中输入文字（支持先清空再输入）。

| 参数 | 说明 |
|------|------|
| 控件变量 | 输入框控件 |
| 输入内容 | 要输入的文字 |
| 先清空 | 输入前是否先清除原内容 |
| 输入方式 | 直接赋值/模拟键盘 |

> **提示**：「直接赋值」速度更快；「模拟键盘」兼容性更好，适合有输入验证的输入框。

---

### 获取文本（desktop_get_text）

读取控件的文本内容（Label、TextBox、状态栏等）。

| 参数 | 说明 |
|------|------|
| 控件变量 | 目标控件 |
| 结果变量 | 保存文本内容 |

---

### 设置值（desktop_set_value）

直接设置控件的值（适合进度条、滑块等特殊控件）。

---

### 操作下拉框（desktop_select_combo）

选择下拉列表（ComboBox）中的选项。

| 参数 | 说明 |
|------|------|
| 控件变量 | 下拉框控件 |
| 选择方式 | 按文本/按索引 |
| 选项文本 | 要选择的选项文字 |
| 选项索引 | 从 0 开始的索引 |

---

### 操作复选框（desktop_checkbox）

勾选或取消勾选复选框。

| 参数 | 说明 |
|------|------|
| 控件变量 | 复选框控件 |
| 操作 | 勾选/取消勾选/切换 |

---

### 操作单选框（desktop_radio）

选中单选按钮。

---

### 拖拽控件（desktop_drag_control）

在控件之间执行拖拽操作。

| 参数 | 说明 |
|------|------|
| 源控件变量 | 拖拽起点控件 |
| 目标控件变量 | 拖拽终点控件 |

---

### 点击菜单（desktop_menu_click）

点击应用菜单栏中的菜单项，支持多级菜单。

| 参数 | 说明 | 示例 |
|------|------|------|
| 应用变量 | 应用句柄 | \`{app}\` |
| 菜单路径 | 用 \`>\` 分隔多级菜单 | \`文件>另存为\` |

**示例**（在记事本中另存为）：

\`\`\`
连接桌面应用 → 进程名: notepad → 结果变量: app
激活窗口 → 应用变量: {app}
点击菜单 → 应用变量: {app}, 菜单路径: 文件>另存为
\`\`\`

---

### 操作列表（desktop_list_operate）

操作 ListBox 或 ListView 控件，支持选中项目、获取列表内容。

| 参数 | 说明 |
|------|------|
| 控件变量 | 列表控件 |
| 操作 | 选中项/获取所有项/获取选中项 |
| 选择方式 | 按文本/按索引 |
| 结果变量 | 保存操作结果 |

---

### 发送按键（desktop_send_keys）

向控件或应用发送键盘按键序列（支持快捷键组合）。

| 参数 | 说明 | 示例 |
|------|------|------|
| 应用/控件变量 | 目标 | \`{app}\` |
| 按键序列 | 使用 \`+\` 组合键 | \`{CTRL}a\` 全选 |

**常用按键格式**：

| 按键 | 格式 |
|------|------|
| Ctrl+C | \`{CTRL}c\` |
| Enter | \`{ENTER}\` |
| Tab | \`{TAB}\` |
| F5 | \`{F5}\` |
| Ctrl+Shift+S | \`{CTRL}{SHIFT}s\` |

---

### 获取属性（desktop_get_property）

获取控件的某个特定属性值（IsEnabled、IsVisible、Value 等）。

| 参数 | 说明 | 示例 |
|------|------|------|
| 控件变量 | 目标控件 | \`{ctrl}\` |
| 属性名 | UIA 属性名 | \`IsEnabled\` |
| 结果变量 | 保存属性值 | \`is_enabled\` |

---

### 处理对话框（desktop_dialog_handle）

自动处理弹出的对话框（确认框、警告框、文件保存框等）。

| 参数 | 说明 |
|------|------|
| 等待超时 | 等待对话框出现（秒） |
| 操作 | 点击确定/取消/关闭/自定义按钮 |
| 自定义按钮文本 | 点击指定文本的按钮 |
| 输入内容 | 如有输入框则填入内容 |
| 结果变量 | 保存对话框文本 |

---

## 📋 完整示例：自动化填写表单

以下示例演示如何自动打开记事本、输入内容并保存：

\`\`\`mermaid
flowchart TD
    A[启动桌面应用\nnotepad.exe] --> B[等待应用就绪]
    B --> C[激活窗口]
    C --> D[查找文本输入框]
    D --> E[输入文本内容]
    E --> F[发送按键 Ctrl+S]
    F --> G[处理对话框\n填入文件名并确定]
    G --> H[关闭桌面应用]
\`\`\`

**完整配置**：

1. **启动桌面应用** → 程序路径: \`C:\\Windows\\notepad.exe\` → 结果变量: \`app\`
2. **等待应用就绪** → 应用变量: \`{app}\` → 超时: \`10\`
3. **激活窗口** → 应用变量: \`{app}\`
4. **查找控件** → 控件类型: \`Edit\` → 结果变量: \`text_box\`
5. **输入文本** → 控件变量: \`{text_box}\` → 内容: \`Hello WebRPA!\`
6. **发送按键** → 应用变量: \`{app}\` → 按键: \`{CTRL}s\`
7. **处理对话框** → 操作: 点击确定
8. **关闭桌面应用** → 应用变量: \`{app}\`

---

## 💡 使用技巧

### 如何获取控件的自动化 ID

1. 点击工具栏「桌面选择器」按钮
2. 将鼠标悬停到目标控件上
3. 底部面板会显示控件的完整属性（Name、AutomationId、ControlType 等）
4. 将这些信息填入「查找控件」模块

### 提高可靠性的建议

- 优先使用 **AutomationId** 查找控件（最稳定，不受语言影响）
- 次选 **控件名称 + 控件类型** 组合
- 在操作前加「等待控件」模块，避免控件未加载时报错
- 操作后加适当「等待」时间，确保操作完成

### 常见问题

**Q：找不到控件怎么办？**
A：用「获取控件树」模块打印应用的控件结构，分析正确的控件属性。

**Q：点击控件没有响应？**
A：尝试先用「激活窗口」将程序置于前台，或改用「发送按键」方式。

**Q：下拉框无法选择？**
A：某些自定义下拉框可能需要先点击展开，再查找并点击选项。
`