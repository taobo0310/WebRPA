export const aiVisionGuideContent = `# 🤖 AI 识别与计算机视觉

本章介绍 OCR 文字识别、验证码识别、人脸识别，以及 AI 智能爬虫模块。

---

## 🔍 OCR 验证码识别（ocr_captcha）

使用 ddddocr 识别网页中常见的图形验证码。

| 参数 | 说明 | 示例 |
|------|------|------|
| 验证码来源 | 元素截图 / 图片路径 / URL | 元素截图 |
| 选择器 | 验证码图片的 CSS 选择器 | \`#captcha-img\` |
| 结果变量 | 保存识别结果 | \`captcha_text\` |

**示例**（自动填写验证码）：
\`\`\`mermaid
flowchart TD
    A[打开登录页] --> B[OCR识别验证码]
    B --> C[输入账号密码和验证码]
    C --> D[点击登录]
\`\`\`

---

## 🧩 滑块验证码（slider_captcha）

自动识别并完成滑动验证码（拖动滑块到缺口位置）。

| 参数 | 说明 |
|------|------|
| 滑块图片选择器 | 滑块图片的 CSS 选择器 |
| 背景图片选择器 | 背景图片的 CSS 选择器 |
| 滑块元素选择器 | 可拖动的滑块 HTML 元素 |
| 结果变量 | 滑动是否成功 |

> 不同网站的滑块结构不同，需根据实际情况调整选择器。

---

## 👁️ 图片文字识别（image_ocr）

使用 EasyOCR 识别图片中的文字，支持 80+ 种语言。

| 参数 | 说明 | 示例 |
|------|------|------|
| 图片路径 | 本地图片或 URL | \`{screenshot_path}\` |
| 识别语言 | 语言代码列表 | \`chi_sim,en\` |
| 返回格式 | 纯文本 / 含坐标详细结果 | 纯文本 |
| 结果变量 | 保存识别文字 | \`ocr_text\` |

常用语言：简中 \`chi_sim\`，繁中 \`chi_tra\`，英文 \`en\`，日文 \`ja\`，韩文 \`ko\`。

---

## 👤 人脸识别（face_recognition）

比对两张图片中的人脸是否为同一人。

| 参数 | 说明 | 示例 |
|------|------|------|
| 已知人脸图片 | 参照图片路径 | \`known.jpg\` |
| 待识别图片 | 要验证的图片 | \`{camera_img}\` |
| 相似度阈值 | 0-1，越小越严格 | \`0.6\` |
| 结果变量 | 含 matched/distance/similarity | \`face_match_result\` |

---

## 🤖 AI 智能爬虫（ai_smart_scraper）

用自然语言描述要提取的数据，AI 自动分析网页并提取，无需手写选择器。

| 参数 | 说明 | 示例 |
|------|------|------|
| 目标 URL | 要爬取的网页地址 | \`https://example.com\` |
| 提取指令 | 自然语言描述 | \`提取所有商品的名称和价格\` |
| LLM 提供商 | OpenAI / 本地模型等 | \`openai\` |
| API Key | 大模型 API 密钥 | \`sk-...\` |
| 结果变量 | 保存提取的结构化数据 | \`scraper_result\` |

> 全局配置中预设 AI 爬虫的 LLM 配置，避免每次重填。

---

## 🎯 AI 元素选择器（ai_element_selector）

用自然语言描述页面元素，AI 自动生成 CSS 选择器。

| 参数 | 说明 | 示例 |
|------|------|------|
| 元素描述 | 用中文描述要找的元素 | \`登录按钮\` |
| URL | 目标页面地址 | \`{current_url}\` |
| 结果变量 | 保存生成的选择器 | \`element_selector\` |

生成的选择器可直接用于点击、输入等其他模块的「选择器」字段。

---

## 🔥 Firecrawl 爬虫

Firecrawl 是专业的 AI 爬虫服务，支持 JS 渲染、智能内容提取。

### 单页爬取（firecrawl_scrape）

爬取单个网页，返回 Markdown 格式的清洁内容。

| 参数 | 说明 |
|------|------|
| URL | 目标网页 |
| API Key | Firecrawl API Key |
| 格式 | markdown / html / links |
| 结果变量 | 保存页面内容 |

### 网站地图（firecrawl_map）

获取整个网站的 URL 列表（站点地图）。

| 参数 | 说明 |
|------|------|
| 根 URL | 网站首页或子目录 |
| 最大链接数 | 最多返回多少个 URL |
| 结果变量 | URL 列表 |

### 全站爬取（firecrawl_crawl）

爬取整个网站的所有页面内容。

| 参数 | 说明 |
|------|------|
| 根 URL | 爬取起点 |
| 最大页面数 | 最多爬取页面数量 |
| 结果变量 | 所有页面内容列表 |

> Firecrawl 需要在 [firecrawl.dev](https://www.firecrawl.dev) 注册并获取 API Key。

---

## 💡 使用技巧

- **OCR 预处理**：对图片先做「图像锐化」再 OCR，可提高准确率
- **验证码重试**：识别失败时刷新验证码重试（条件判断 + 循环）
- **人脸识别阈值**：一般设 0.5-0.6，过小会误判，过大会漏判
- **AI 爬虫提示词**：提示词越具体效果越好，如「提取 li 标签中的商品名称文字」
`
