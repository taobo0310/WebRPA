export const inputGuideContent = `# 🖱️ 屏幕、鼠标与键盘模拟

本章介绍屏幕截图、录像，以及真实鼠标键盘模拟操作模块。

---

## 📷 屏幕操作

### 屏幕截图（screenshot_screen）

截取当前屏幕图像并保存为文件。

| 参数 | 说明 | 示例 |
|------|------|------|
| 保存路径 | 图片保存路径 | \`C:\\screenshots\\\` |
| 区域截图 | 是否只截取部分屏幕 | 否 |
| X/Y/宽/高 | 截取区域 | \`0,0,1920,1080\` |
| 结果变量 | 保存图片路径 | \`screen_path\` |

---

### 录制屏幕（screen_record）

录制屏幕操作为视频文件。

| 参数 | 说明 |
|------|------|
| 保存路径 | 视频保存路径 |
| 帧率 | 每秒帧数（10-60） |
| 录制时长 | 秒数（0 表示手动停止） |

---

### 窗口聚焦（window_focus）

将指定标题的窗口置于前台并激活。

| 参数 | 说明 |
|------|------|
| 窗口标题 | 支持模糊匹配 |

---

### 摄像头拍照（camera_capture）

使用电脑摄像头拍摄一张照片。

| 参数 | 说明 |
|------|------|
| 摄像头索引 | 0 为默认摄像头 |
| 保存路径 | 图片保存路径 |
| 结果变量 | 保存图片路径 |

---

### 摄像头录像（camera_record）

使用摄像头录制视频。

| 参数 | 说明 |
|------|------|
| 摄像头索引 | 0 为默认 |
| 录制时长（秒） | 录制时长 |
| 保存路径 | 视频保存路径 |

---

## 🖱️ 真实鼠标模拟

真实鼠标模块通过操作系统 API 模拟实际鼠标动作，适用于无法用浏览器自动化的桌面软件场景。

### 真实鼠标点击（real_mouse_click）

在屏幕指定坐标处执行鼠标点击。

| 参数 | 说明 | 示例 |
|------|------|------|
| X/Y 坐标 | 屏幕像素坐标 | \`960, 540\` |
| 按键 | left / right / middle | \`left\` |
| 点击次数 | 1（单击）/ 2（双击） | \`1\` |
| 点击后等待（毫秒） | 点击后暂停 | \`100\` |

> **获取坐标**：工具栏「坐标获取」按钮，移动鼠标后点击即可复制坐标。

---

### 真实鼠标移动（real_mouse_move）

将鼠标光标平滑移动到指定位置。

| 参数 | 说明 |
|------|------|
| X/Y 坐标 | 目标坐标 |
| 移动时长（毫秒） | 0 为瞬间移动 |

---

### 真实鼠标拖拽（real_mouse_drag）

从一个坐标拖拽到另一个坐标。

| 参数 | 说明 |
|------|------|
| 起点 X/Y | 拖拽起始坐标 |
| 终点 X/Y | 拖拽目标坐标 |
| 拖拽时长（毫秒） | 拖拽动画时长 |

---

### 真实鼠标滚动（real_mouse_scroll）

在指定位置滚动鼠标滚轮。

| 参数 | 说明 |
|------|------|
| X/Y 坐标 | 滚动位置 |
| 滚动量 | 正数向上，负数向下 |

---

### 获取鼠标位置（get_mouse_position）

获取当前鼠标光标的屏幕坐标。

| 参数 | 说明 |
|------|------|
| 结果变量 | 保存坐标字典（含 x 和 y） |

---

## ⌨️ 真实键盘模拟

### 真实键盘操作（real_keyboard）

模拟真实键盘输入文字或按下快捷键。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 操作类型 | 输入文字 / 按下按键 / 组合键 | 输入文字 |
| 输入内容 | 要输入的文字 | \`Hello WebRPA\` |
| 按键名称 | 按键标识符 | \`enter\` |
| 组合键 | 多键组合 | \`ctrl+c\` |
| 每键间隔（毫秒） | 模拟人工输入速度 | \`50\` |

**常用按键名称**：

| 按键 | 名称 | 按键 | 名称 |
|------|------|------|------|
| 回车 | enter | 退格 | backspace |
| Tab | tab | Esc | escape |
| 上/下/左/右 | up/down/left/right | Delete | delete |
| F1-F12 | f1-f12 | Home/End | home/end |
| Page Up/Down | pageup/pagedown | 空格 | space |

**常用组合键**：

| 操作 | 组合键 |
|------|--------|
| 全选 | ctrl+a |
| 复制 | ctrl+c |
| 粘贴 | ctrl+v |
| 保存 | ctrl+s |
| 撤销 | ctrl+z |
| 截图 | win+shift+s |

---

### 模拟按键（keyboard_action）

向当前焦点窗口发送按键事件（基于 PyAutoGUI）。

| 参数 | 说明 |
|------|------|
| 按键 | 按键名称或组合 |
| 按下次数 | 重复次数 |

---

## 🖼️ 图像/文字识别点击

### 点击图像（click_image）

在屏幕上找到指定图像并点击，无需坐标，自动定位。

| 参数 | 说明 | 示例 |
|------|------|------|
| 模板图像 | 要查找的图片（从图像资源选择） | 按钮截图.png |
| 相似度 | 匹配阈值（0-1） | \`0.8\` |
| 等待超时（秒） | 最长等待时间 | \`10\` |
| 偏移 X/Y | 点击位置偏移量 | \`0, 0\` |

---

### 图像存在判断（image_exists）

检查屏幕上是否存在某个图像。

| 参数 | 说明 |
|------|------|
| 模板图像 | 要查找的图片 |
| 相似度 | 匹配阈值 |
| 结果变量 | True/False |

---

### 点击文字（click_text）

通过 OCR 在屏幕上找到指定文字并点击。

| 参数 | 说明 |
|------|------|
| 目标文字 | 要点击的文字 |
| 语言 | 识别语言 |

---

### 悬停图像/文字（hover_image / hover_text）

将鼠标悬停到图像或文字位置（不点击）。

---

### 拖拽图像（drag_image）

找到图像后，将其拖拽到另一个位置。

---

## 🎹 宏录制器（macro_recorder）

录制一段鼠标键盘操作，然后自动重放，是最简单的桌面自动化方式。

**使用步骤**：

1. 拖入「宏录制器」模块
2. 点击模块的「开始录制」按钮
3. 手动执行需要自动化的操作（鼠标点击、键盘输入等）
4. 点击「停止录制」
5. 工作流执行时会精确重放录制的操作序列

**配置项**：

| 参数 | 说明 |
|------|------|
| 录制内容 | 已录制的操作序列（自动填入）|
| 回放速度 | 1.0 为原速，2.0 为两倍速 |
| 重复次数 | 重放次数 |

---

## 💡 使用技巧

- **坐标系**：所有坐标都是屏幕绝对坐标（以屏幕左上角为原点）
- **多显示器**：第二块屏幕的坐标从第一块宽度开始（如主屏 1920px，副屏 X 从 1920 开始）
- **分辨率适配**：硬编码坐标会因分辨率不同而失效，推荐用「点击图像」代替固定坐标
- **执行前聚焦**：鼠标键盘操作前，先用「窗口聚焦」确保目标窗口在前台
- **操作间加等待**：每个操作后加 100-500ms 等待，提高稳定性
`
