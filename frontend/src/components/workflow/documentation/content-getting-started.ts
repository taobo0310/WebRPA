export const gettingStartedContent = `# 🚀 快速入门：5分钟学会WebRPA

欢迎来到**WebRPA**（Web Automation Framework）！这是一个强大且易用的网页自动化工具，让你无需编程也能轻松实现各种自动化任务。

---

## 📌 重要更新：浏览器引擎架构

WebRPA 现已采用**主进程浏览器引擎**架构，所有工作流执行时可以直接复用已打开的浏览器页面，无需重启浏览器。

**新的工作流执行流程：**
1. 点击工具栏"打开浏览器"按钮，在主进程中启动浏览器
2. 手动打开需要操作的网站
3. 创建工作流，第一个模块使用"操作已打开的网页"
4. 运行工作流 → 直接操控已打开的页面，无需重启

**优势：**
- ⚡ 提高自动化效率（无需重启浏览器）
- 💾 节省系统资源（共享浏览器实例）
- 🔄 支持多页面操作（自动管理页面生命周期）

---

## 🎯 WebRPA能做什么？

### 日常自动化
- 📝 每天自动登录网站签到领积分
- 🛒 监控商品价格，降价自动通知
- 📧 自动发送邮件通知
- 🔄 定时执行重复性任务

### 数据采集
- 📊 批量采集网页数据导出Excel
- 📰 自动抓取新闻、文章内容
- 🏷️ 采集商品信息、价格、评论
- 📋 提取表格、列表数据
- 🗄️ 采集数据直接入库MySQL

### 智能处理
- 🤖 结合AI智能处理和分析数据
- 👁️ AI视觉识别验证码、图片内容
- 📝 自动生成报告和摘要

---

## 🏃 第一个工作流：百度搜索

让我们从一个简单的例子开始，创建一个自动在百度搜索的工作流。

### 第1步：打开浏览器

1. 点击工具栏的"打开浏览器"按钮
2. 在弹出的浏览器中手动打开 https://www.baidu.com

### 第2步：拖入"操作已打开的网页"模块

1. 在左侧**模块列表**中找到 **🌐 操作已打开的网页**
2. 用鼠标**拖动**它到中间的画布上
3. 这个模块会自动连接到已打开的百度页面

### 第3步：添加"输入文本"模块

1. 拖入 **⌨️ 输入文本** 模块到画布
2. **连接模块**：将鼠标移到"操作已打开的网页"模块底部的小圆点上，按住拖动到"输入文本"模块顶部的小圆点，松开即可连接
3. 配置"输入文本"模块：
   - **选择器**：\`#kw\`（这是百度搜索框的ID）
   - **输入内容**：\`WebRPA 自动化工具\`

### 第4步：添加"点击元素"模块

1. 拖入 **🖱️ 点击元素** 模块
2. 连接到"输入文本"模块
3. 配置：
   - **选择器**：\`#su\`（这是百度搜索按钮的ID）

### 第4步：运行工作流

1. 点击顶部工具栏的 **▶️ 运行** 按钮
2. 观察浏览器自动打开百度、输入文字、点击搜索
3. 在底部**日志面板**查看执行过程

🎉 恭喜！你已经完成了第一个自动化工作流！

### 工作流程图

\`\`\`mermaid
%%{init: {'theme':'default', 'themeVariables': { 'fontSize':'18px'}}}%%
graph LR
    A["<b>打开网页</b><br/><br/>百度首页"] --> B["<b>输入文本</b><br/><br/>搜索框输入关键词"]
    B --> C["<b>点击元素</b><br/><br/>点击搜索按钮"]
    C --> D["<b>完成搜索</b>"]
    
    style A fill:#e3f2fd,stroke:#1976d2,stroke-width:3px,color:#000
    style B fill:#fff3e0,stroke:#f57c00,stroke-width:3px,color:#000
    style C fill:#f3e5f5,stroke:#7b1fa2,stroke-width:3px,color:#000
    style D fill:#e8f5e9,stroke:#388e3c,stroke-width:3px,color:#000
\`\`\`

---

## 📌 界面详细介绍

### 整体布局

\`\`\`
┌─────────────────────────────────────────────────────────┐
│                    顶部工具栏                            │
├──────────┬────────────────────────────┬─────────────────┤
│          │                            │                 │
│  模块    │         画布区域            │    配置面板     │
│  列表    │    （搭建工作流的地方）       │   （参数设置）   │
│          │                            │                 │
├──────────┴────────────────────────────┴─────────────────┤
│                    日志面板                              │
└─────────────────────────────────────────────────────────┘
\`\`\`

### 1. 左侧模块列表

所有可用的功能模块，按类别分组：
- **基础操作**：打开网页、点击、输入、获取信息等
- **元素操作**：获取子元素、获取兄弟元素
- **等待模块**：固定等待、等待元素
- **数据处理**：变量、列表、字典操作
- **数据表格**：添加行列、导出Excel
- **数据库**：MySQL连接、增删改查
- **流程控制**：条件判断、循环、遍历、定时执行、子流程
- **AI能力**：AI对话、AI视觉、AI智能爬虫、AI元素选择器
- **高级功能**：API请求、邮件、文件操作、PDF处理、媒体处理等

### 2. 中间画布区域

- **模块数量**：左上角实时显示当前画布中的模块数量
- **拖拽模块**：从左侧拖入模块
- **连接模块**：从一个模块的输出点拖到另一个模块的输入点
- **选择模块**：单击选中，可在右侧配置
- **删除模块**：选中后按 Delete 键
- **移动视图**：按住空格键拖动，或使用鼠标滚轮缩放

### 3. 右侧配置面板

选中模块后显示该模块的配置项：
- **模块名称**：可自定义，方便识别
- **具体参数**：根据模块类型不同而不同
- **高级选项**：超时时间（实际生效）、重试次数等
- **变量名输入**：支持模糊搜索自动补全，输入时会显示匹配的变量列表

### 4. 底部日志面板

四个标签页：
- **📋 执行日志**：执行过程的详细日志，支持搜索和筛选，可设置显示条数（100-500条）
- **📊 数据表格**：采集到的数据，可编辑、添加行列、导出CSV
- **📦 全局变量**：当前所有变量的值，可添加和编辑
- **💾 Excel资源**：上传的Excel文件，供工作流读取使用

### 5. 顶部工具栏

| 按钮 | 功能 |
|------|------|
| ▶️ 运行 | 开始执行工作流 |
| ⏹️ 停止 | 停止正在执行的工作流 |
| 💾 保存 | 保存工作流到本地文件夹（Ctrl+S） |
| 📂 打开 | 打开本地保存的工作流 |
| 📦 工作流仓库 | 浏览、下载、发布工作流 |
| 🌐 自动化浏览器 | 管理自动化浏览器实例 |
| 📖 教学文档 | 打开本教学文档 |
| ⚙️ 全局配置 | 配置AI、邮件、工作流存储路径等 |
| 🗑️ 清空 | 清空当前工作流 |

---

## 🔗 变量引用快速入门

在任何输入框中，都可以使用 \`{变量名}\` 来引用变量的值。输入 \`{\` 会自动弹出变量提示列表。

### 变量名输入框自动补全

所有变量名输入框都支持**模糊搜索自动补全**功能：
- 输入时会自动显示匹配的变量列表
- 支持模糊匹配，输入部分字符即可找到变量
- 使用 ↑↓ 键选择，Enter 确认，Esc 关闭
- 存储变量名输入框（用于创建新变量）不会提示"变量不存在"

### 基础语法

| 语法 | 说明 | 示例 |
|------|------|------|
| \`{变量名}\` | 引用变量值 | \`{用户名}\` → 小明 |
| \`{列表[索引]}\` | 列表取值 | \`{水果[0]}\` → 苹果 |
| \`{列表[-1]}\` | 列表最后一项 | \`{水果[-1]}\` → 橙子 |
| \`{字典[键]}\` | 字典取值 | \`{用户[name]}\` → 小明 |
| \`{数据[0][title]}\` | 嵌套访问 | 列表中字典的字段 |

### 循环变量

循环模块会自动创建变量，可以直接引用：
- \`{item}\` - 遍历列表时的当前元素
- \`{index}\` - 遍历列表时的当前索引
- \`{loop_index}\` - 计数循环时的循环索引

### 在文本中混合使用

\`\`\`
你好，{用户名}！你的订单号是 {订单号}，共 {金额} 元。
\`\`\`

---

## 🔥 新手必知的小贴士

### 元素选择器
- **Ctrl+点击** 选择器按钮 🎯：可视化选择单个元素
- **Shift+点击** 选择器按钮：选择相似元素（批量采集必备）

### 快捷操作
- **Ctrl+S**：保存当前工作流到本地
- **Alt+N**：新建工作流（清空画布）
- **Ctrl+C / Ctrl+V**：复制粘贴模块
- **Ctrl+Z**：撤销操作
- **Ctrl+Y**：重做操作
- **Ctrl+A**：全选所有模块
- **Ctrl+D**：禁用/启用选中的模块
- **Ctrl+F**：搜索模块（按名称或备注）
- **Ctrl+G**：跳转到下一个搜索结果
- **Delete / Backspace**：删除选中的模块
- 拖动画布空白处可以移动视图
- 滚轮缩放画布
- 拖拽JSON文件到画布可直接导入工作流

### 调试技巧
- 遇到问题先看**日志面板**的错误信息
- 使用**打印日志**模块输出变量值
- 先测试单个模块，再测试完整流程
- 使用**变量追踪**功能查看变量的完整变化历史

### 全局配置
- 在工具栏点击**⚙️ 全局配置**可以设置默认值
- 配置AI、邮件、数据库等模块的默认参数
- 设置工作流保存路径和自动保存选项
- 启用**自动识别剪贴板截图**：当你按 PrtScn 或使用截图工具后，会自动弹出保存对话框，方便快速保存截图到指定位置

### 稳定性
- 页面跳转后加**等待**模块
- 合理设置**超时时间**和**重试次数**
- 使用**等待元素**比固定等待更智能

---

## 📚 下一步学习

1. **基础模块详解** - 了解每个模块的详细用法
2. **数据处理指南** - 学习变量、列表、字典操作
3. **数据库操作** - 学习MySQL数据库连接与操作
4. **选择器完全指南** - 掌握CSS选择器
5. **实战案例** - 学习完整的自动化方案
6. **高级功能** - 探索AI、API等高级特性

---

💡 **提示**：学习自动化最好的方式就是动手实践！试着创建一个自动登录你常用网站的工作流吧！`
