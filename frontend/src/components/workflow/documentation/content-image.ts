export const imageGuideContent = `# 🖼️ 图像处理

本章介绍 WebRPA 提供的图像编辑与处理模块，基于 Pillow（PIL）库实现。

---

## 📐 图像变换

### 图像缩放（image_resize）

调整图像的尺寸。

| 参数 | 说明 | 示例 |
|------|------|------|
| 输入图像 | 原图路径或变量 | \`{img_path}\` |
| 宽度/高度 | 目标尺寸（像素） | \`800, 600\` |
| 保持比例 | 只需填宽或高之一 | 是 |
| 结果变量 | 保存输出路径 | \`resized_image\` |

### 图像裁剪（image_crop）

裁剪图像的指定区域。

| 参数 | 说明 |
|------|------|
| 左/上/右/下 | 裁剪区域坐标（像素） |
| 结果变量 | 保存裁剪后图片路径 |

### 图像旋转（image_rotate）

| 参数 | 说明 |
|------|------|
| 旋转角度 | 顺时针角度（0-360） |
| 展开画布 | 旋转后是否扩大画布 |
| 结果变量 | 保存旋转后路径 |

### 图像翻转（image_flip）

水平或垂直翻转图像（镜像效果）。

| 参数 | 说明 |
|------|------|
| 翻转方向 | horizontal / vertical |
| 结果变量 | 保存翻转后路径 |

### 生成缩略图（image_thumbnail）

快速生成缩略图，保持宽高比。

| 参数 | 说明 |
|------|------|
| 最大宽度/高度 | 缩略图最大尺寸 |
| 结果变量 | 保存缩略图路径 |

---

## 🎨 图像调色

### 亮度调整（image_brightness）

| 参数 | 说明 |
|------|------|
| 亮度系数 | 1.0 原始，>1 变亮，<1 变暗 |
| 结果变量 | \`brightness_image\` |

### 对比度调整（image_contrast）

| 参数 | 说明 |
|------|------|
| 对比度系数 | 1.0 原始，>1 增强 |
| 结果变量 | \`contrast_image\` |

### 色彩平衡（image_color_balance）

调整饱和度：0 灰度，1 原始，>1 增强饱和度。

### 灰度化（image_grayscale）

将彩色图像转为黑白灰度图像。

### 图像滤镜（image_filter）

| 滤镜 | 效果 |
|------|------|
| BLUR | 高斯模糊 |
| SHARPEN | 锐化 |
| EDGE_ENHANCE | 边缘增强 |
| EMBOSS | 浮雕效果 |
| SMOOTH | 平滑 |

### 图像模糊（image_blur）

对图像应用高斯模糊，半径越大越模糊。

### 图像锐化（image_sharpen）

增强图像清晰度和边缘细节。

### 圆角处理（image_round_corners）

为图像添加圆角效果。

| 参数 | 说明 |
|------|------|
| 圆角半径 | 像素，越大越圆 |
| 结果变量 | 保存路径 |

---

## 📝 图像内容操作

### 图像添加文字（image_add_text）

在图像上叠加文字。

| 参数 | 说明 | 示例 |
|------|------|------|
| 文字内容 | 要添加的文字 | \`WebRPA\` |
| X/Y 位置 | 文字起始坐标 | \`10, 10\` |
| 字体大小 | 像素 | \`36\` |
| 颜色 | 颜色名称或 hex | \`red\` |
| 结果变量 | 保存路径 | \`text_image\` |

### 添加水印（add_watermark）

在图像上叠加图片水印（如 Logo）。

| 参数 | 说明 |
|------|------|
| 水印图像 | 水印图片路径 |
| 位置 | topleft/topright/bottomleft/bottomright/center |
| 透明度 | 0-1（1 不透明） |
| 结果变量 | 保存路径 |

### 图像拼接（image_merge）

将多张图片拼接为一张。

| 参数 | 说明 |
|------|------|
| 图片列表 | 多个图片路径（用换行分隔） |
| 拼接方向 | horizontal（横向）/ vertical（纵向） |
| 间距 | 图片之间的间距（像素） |
| 结果变量 | 保存拼接后路径 |

### 简单去背景（image_remove_bg）

基于颜色阈值去除图像背景。

| 参数 | 说明 |
|------|------|
| 背景颜色 | 要去除的颜色（默认白色） |
| 容差 | 颜色相似度容差（0-255） |
| 结果变量 | 保存透明 PNG 路径 |

---

## 🛠️ 图像工具

### 获取图像信息（image_get_info）

获取图像的宽度、高度、格式、文件大小等信息。

| 参数 | 说明 |
|------|------|
| 输入图像 | 图片路径 |
| 结果变量 | 保存信息字典（width/height/format/size 等） |

### 图像格式转换（image_convert_format）

转换图像格式（JPG/PNG/WebP/BMP/GIF 等）。

| 参数 | 说明 | 示例 |
|------|------|------|
| 输入图像 | 原图路径 | \`{img_path}\` |
| 目标格式 | 输出格式 | \`webp\` |
| 质量 | 1-100（JPG/WebP 有效） | \`85\` |
| 结果变量 | 保存转换后路径 | \`converted_image\` |

### 二维码生成（qr_generate）

生成二维码图片。

| 参数 | 说明 | 示例 |
|------|------|------|
| 内容 | 要编码的文字或 URL | \`https://example.com\` |
| 大小 | 图片尺寸（像素） | \`300\` |
| 容错级别 | L/M/Q/H | \`M\` |
| 保存路径 | 输出路径 | \`C:\\qr.png\` |
| 结果变量 | 保存路径 | \`qr_path\` |

### 二维码解码（qr_decode）

从图片中识别并解码二维码或条形码。

| 参数 | 说明 |
|------|------|
| 输入图像 | 含二维码的图片路径 |
| 结果变量 | 保存解码内容（字符串） |

---

## 🔄 格式工厂

格式工厂模块支持批量转换图片、视频、音频格式。

### 图片格式转换（image_format_convert）

| 参数 | 说明 |
|------|------|
| 输入路径 | 图片路径或文件夹 |
| 目标格式 | jpg/png/webp/bmp/gif |
| 质量 | 压缩质量（1-100） |
| 结果变量 | 保存转换后路径 |

### 视频格式转换（video_format_convert）

| 参数 | 说明 |
|------|------|
| 输入路径 | 视频文件路径 |
| 目标格式 | mp4/avi/mkv/mov/webm 等 |
| 视频编码 | h264/h265/vp9 等 |
| 结果变量 | 保存转换后路径 |

### 音频格式转换（audio_format_convert）

| 参数 | 说明 |
|------|------|
| 输入路径 | 音频文件路径 |
| 目标格式 | mp3/aac/flac/wav/ogg 等 |
| 码率 | 128k/192k/320k 等 |
| 结果变量 | 保存转换后路径 |

### 视频转 GIF（video_to_gif）

| 参数 | 说明 |
|------|------|
| 输入视频 | 视频文件路径 |
| 开始时间 | 秒数 |
| 持续时长 | 秒数 |
| 帧率 | GIF 帧率（10-15 推荐） |
| 宽度 | GIF 宽度（像素） |
| 结果变量 | 保存 GIF 路径 |

### 批量格式转换（batch_format_convert）

将整个文件夹内的媒体文件批量转换格式。

| 参数 | 说明 |
|------|------|
| 输入文件夹 | 源文件夹路径 |
| 媒体类型 | image/video/audio |
| 目标格式 | 目标格式 |
| 输出文件夹 | 转换后保存位置 |
| 结果变量 | 成功转换的文件列表 |

---

## 💡 使用技巧

- 所有图像处理模块的**输出路径**留空时，会在原文件同目录下生成新文件
- 批量处理时，结合「遍历列表」模块和「获取文件列表」模块
- 图片质量与文件大小成正比，一般 75-85 质量兼顾效果与大小
- 格式转换优先推荐 WebP 格式，体积更小、质量更好
`
