export const shareGuideContent = `# 🔗 网络共享与屏幕共享

本章介绍文件夹共享、文件共享和屏幕投屏功能。

---

## 📁 网络共享

### 共享文件夹（share_folder）

将本地文件夹通过局域网共享，让其他设备可以访问。

| 参数 | 说明 | 示例 |
|------|------|------|
| 文件夹路径 | 要共享的本地文件夹 | \`C:\\shared\` |
| 共享名称 | 共享后的访问名称 | \`myshare\` |
| 端口 | HTTP 服务端口 | \`8888\` |
| 结果变量 | 保存访问 URL | \`share_url\` |

共享后，同一局域网内的设备可以通过浏览器访问 \`http://你的IP:端口\` 浏览和下载文件。

---

### 共享单个文件（share_file）

将单个文件共享为可下载链接。

| 参数 | 说明 |
|------|------|
| 文件路径 | 要共享的文件 |
| 端口 | HTTP 服务端口 |
| 结果变量 | 保存下载链接 |

---

### 停止共享（stop_share）

关闭文件共享服务。

| 参数 | 说明 |
|------|------|
| 端口 | 要关闭的服务端口 |

---

## 🖥️ 屏幕共享

### 启动屏幕共享（start_screen_share）

将本机屏幕通过局域网实时共享，其他设备可用浏览器观看。

| 参数 | 说明 | 示例 |
|------|------|------|
| 端口 | 共享服务端口 | \`9999\` |
| 帧率 | 每秒帧数（影响流畅度和带宽） | \`15\` |
| 质量 | 图像质量（1-100） | \`70\` |
| 结果变量 | 保存观看 URL | \`share_url\` |

访问方式：在同一局域网内打开浏览器，访问 \`http://主机IP:端口\` 即可观看实时屏幕。

---

### 停止屏幕共享（stop_screen_share）

关闭屏幕共享服务。

| 参数 | 说明 |
|------|------|
| 端口 | 要关闭的共享端口 |

---

## 💡 使用场景

- **自动化监控**：启动屏幕共享后，远程查看工作流执行进度
- **文件分发**：将生成的报告文件共享给局域网内其他用户下载
- **临时传输**：在两台电脑间快速传输大文件，无需 U 盘或网盘
`
