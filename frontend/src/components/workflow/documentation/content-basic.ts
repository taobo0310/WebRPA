export const basicModulesContent = `# ⚡ 基础模块详解

本章详细介绍所有基础模块的功能和配置方法。

---

## 🌐 浏览器操作模块

### 打开网页

最常用的起始模块，用于打开指定的网页。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 网址 | 要打开的URL | \`https://www.baidu.com\` |
| 等待条件 | 页面加载完成的判断方式 | load / domcontentloaded |
| 超时时间 | 最长等待时间（毫秒） | 60000 |

**等待条件说明**：
- **load**：等待所有资源（图片、样式等）加载完成
- **domcontentloaded**：DOM加载完成即可，更快
- **networkidle**：网络空闲时，适合Ajax页面

**使用变量**：
\`\`\`
网址：https://www.example.com/user/{用户ID}
\`\`\`

---

### 点击元素

模拟鼠标点击网页元素。

**配置项**：

| 参数 | 说明 | 可选值 |
|------|------|--------|
| 选择器 | 目标元素的CSS选择器 | \`#btn\`、\`.submit\` |
| 点击类型 | 点击方式 | 单击/双击/右键 |
| 等待可点击 | 是否等待元素可点击 | 是/否 |

**点击类型详解**：
- **单击**：普通左键点击，最常用
- **双击**：快速点击两次，用于打开文件等
- **右键**：弹出右键菜单

**常见问题**：
- 点击无效？尝试先添加"等待元素"模块
- 元素被遮挡？使用"滚动页面"后再点击

---

### 悬停元素

将鼠标移动到指定元素上方并悬停。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 选择器 | 目标元素的CSS选择器 | \`.menu-item\` |
| 悬停时长 | 鼠标悬停的时间（毫秒） | 500 |

**使用场景**：
- 触发下拉菜单显示
- 触发悬停提示（tooltip）
- 触发鼠标悬停动画效果
- 配合点击实现二级菜单操作

**示例**：打开二级菜单
\`\`\`
1. 悬停元素：.menu-item（悬停500ms）
2. 等待元素：.submenu 出现
3. 点击元素：.submenu-item
\`\`\`

---

### 输入文本

在输入框中填写内容。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 选择器 | 输入框的CSS选择器 | \`#username\` |
| 输入内容 | 要填写的文本 | \`{用户名}\` |
| 清空原有内容 | 输入前是否先清空 | 是/否 |

**特殊字符输入**：
- 换行：使用 \`\\n\`
- Tab：使用 \`\\t\`

---

### 获取元素信息

从网页元素中提取数据，是数据采集的核心模块。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 选择器 | 目标元素的CSS选择器 | \`.product-title\` |
| 提取类型 | 要提取的内容类型 | 文本/属性/HTML |
| 属性名 | 提取属性时的属性名 | href、src |
| 保存到变量 | 存储结果的变量名 | 商品标题 |
| 存储的列名 | 自动采集到数据表格的列名（支持变量引用） | 标题、\`{动态列名}\` |

**存储的列名支持变量引用**：
- 可以使用固定的列名：\`商品名称\`、\`价格\`
- 也可以使用变量动态指定列名：\`{列名变量}\`、\`第{index}列\`
- 适合需要动态生成列名的场景

**提取类型详解**：

1. **文本内容**：获取元素显示的文字
   \`\`\`html
   <span class="price">¥99.00</span>
   → 提取结果：¥99.00
   \`\`\`

2. **属性值**：获取元素的HTML属性
   \`\`\`html
   <a href="https://example.com">链接</a>
   → 提取 href 属性：https://example.com
   \`\`\`

3. **HTML内容**：获取元素的完整HTML
   \`\`\`html
   <div class="content"><p>段落</p></div>
   → 提取结果：<p>段落</p>
   \`\`\`

**数据采集技巧**：
- 填写"存储的列名"后，数据会自动收集到数据表格
- 配合循环使用可批量采集多条数据
- 在日志面板的"数据表格"标签页可预览、编辑和导出

---

### 获取子元素

获取指定元素下的所有子元素，返回子元素列表。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 父元素选择器 | 父元素的CSS选择器 | \`.product-list\` |
| 子元素选择器 | 子元素的CSS选择器（相对于父元素） | \`.item\` |
| 保存到变量 | 存储子元素列表的变量名 | 商品列表 |

**使用场景**：
- 获取列表中的所有项目
- 获取容器内的所有卡片
- 批量处理子元素

**返回结果**：
返回一个列表，每个元素包含：
\`\`\`json
[
  {
    "text": "元素文本内容",
    "html": "<div>元素HTML</div>",
    "selector": ".item:nth-child(1)"
  },
  {
    "text": "元素文本内容2",
    "html": "<div>元素HTML2</div>",
    "selector": ".item:nth-child(2)"
  }
]
\`\`\`

**示例**：获取商品列表
\`\`\`
获取子元素：
  父元素选择器：.product-container
  子元素选择器：.product-item
  保存到变量：商品列表
  
遍历列表：
  数据源：商品列表
  
  循环体：
    打印日志：{item[text]}
\`\`\`

---

### 获取兄弟元素

获取指定元素的所有兄弟元素（同级元素），返回兄弟元素列表。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 元素选择器 | 参考元素的CSS选择器 | \`.current-item\` |
| 兄弟元素选择器 | 兄弟元素的CSS选择器（可选） | \`.item\` |
| 保存到变量 | 存储兄弟元素列表的变量名 | 兄弟元素 |

**使用场景**：
- 获取同级的其他选项
- 获取表格同行的其他单元格
- 处理平级关系的元素

**返回结果**：
返回一个列表，每个元素包含：
\`\`\`json
[
  {
    "text": "兄弟元素文本",
    "html": "<div>兄弟元素HTML</div>",
    "selector": ".item:nth-child(2)"
  }
]
\`\`\`

**注意事项**：
- 不包含参考元素本身
- 如果指定了兄弟元素选择器，只返回匹配的兄弟元素
- 如果不指定兄弟元素选择器，返回所有兄弟元素

**示例**：获取表格行的其他单元格
\`\`\`
获取兄弟元素：
  元素选择器：td.name-cell
  兄弟元素选择器：td
  保存到变量：其他单元格
  
遍历列表：
  数据源：其他单元格
  
  循环体：
    打印日志：{item[text]}
\`\`\`

---

### 关闭页面

关闭当前浏览器页面。

---

## ⏰ 等待模块

### 等待（固定时间）

暂停执行指定的时间。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 等待时间 | 暂停的毫秒数 | 2000（2秒） |

**使用场景**：
- 等待动画完成
- 等待异步请求返回
- 避免操作过快被检测
- 给页面加载预留时间

**注意**：固定等待不够智能，建议优先使用"等待元素"。

---

### 等待元素

等待某个元素满足指定条件，比固定等待更智能。

**配置项**：

| 参数 | 说明 | 可选值 |
|------|------|--------|
| 选择器 | 目标元素的CSS选择器 | \`#loading\` |
| 等待条件 | 元素需要满足的条件 | 出现/消失/可见/可点击 |
| 超时时间 | 最长等待时间（毫秒） | 10000 |

**等待条件详解**：

| 条件 | 说明 | 使用场景 |
|------|------|----------|
| 出现 | 元素存在于DOM中 | 等待内容加载 |
| 消失 | 元素从DOM中移除 | 等待加载动画消失 |
| 可见 | 元素可见（非隐藏） | 等待弹窗显示 |
| 可点击 | 元素可以被点击 | 等待按钮激活 |

**示例**：等待加载完成
\`\`\`
1. 等待元素：.loading-spinner 消失
2. 然后执行后续操作
\`\`\`

---

## 📝 表单操作模块

### 下拉框选择

选择下拉菜单（\`<select>\`）的选项。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 选择器 | 下拉框的CSS选择器 | \`#city-select\` |
| 选择方式 | 如何定位选项 | 按值/按文本/按索引 |
| 选项值 | 要选择的选项 | beijing / 北京 / 0 |

**选择方式详解**：

1. **按值选择**：使用 option 的 value 属性
   \`\`\`html
   <option value="beijing">北京</option>
   → 选项值填：beijing
   \`\`\`

2. **按文本选择**：使用 option 显示的文字
   \`\`\`html
   <option value="beijing">北京</option>
   → 选项值填：北京
   \`\`\`

3. **按索引选择**：使用选项的位置（从0开始）
   \`\`\`
   第一个选项：0
   第二个选项：1
   \`\`\`

---

### 设置复选框

勾选或取消勾选复选框。

**配置项**：

| 参数 | 说明 |
|------|------|
| 选择器 | 复选框的CSS选择器 |
| 操作 | 勾选/取消勾选 |

---

### 拖拽元素

将一个元素拖动到另一个位置。

**配置项**：

| 参数 | 说明 |
|------|------|
| 源元素选择器 | 要拖动的元素 |
| 目标选择器 | 拖动到的目标元素 |
| 目标坐标 | 或者指定目标坐标 |

---

### 上传文件

选择文件上传，支持两种触发方式。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 选择器 | 文件输入框或上传按钮的选择器 | \`input[type="file"]\` 或 \`#upload-btn\` |
| 文件路径 | 要上传的文件路径 | \`C:/files/image.jpg\` |

**支持的元素类型**：
- **input[type="file"]**：标准文件输入框，直接设置文件
- **按钮元素**：点击后弹出文件选择器的按钮，自动监听文件选择器

**路径选择**：点击输入框右侧的 📁 按钮可以通过资源管理器选择文件

**注意**：不支持选择文件夹的上传控件（webkitdirectory）

---

## 📜 滚动模块

### 滚动页面

控制页面滚动。

**配置项**：

| 参数 | 说明 | 可选值 |
|------|------|--------|
| 滚动方向 | 滚动的方向 | 上/下/左/右 |
| 滚动距离 | 滚动的像素数 | 500 |
| 滚动方式 | 滚动的实现方式 | 自动/鼠标滚轮/脚本滚动 |

**滚动方式详解**：
- **自动（推荐）**：优先使用鼠标滚轮，失败则用脚本
- **鼠标滚轮**：模拟真实滚轮事件，适合抖音等虚拟滚动页面
- **脚本滚动**：传统JavaScript滚动，适合普通网页

**注意**：抖音、小红书等使用虚拟滚动的页面，请选择"鼠标滚轮"模式。

**懒加载页面采集**：
\`\`\`
循环执行 10 次
  ├─ 滚动页面（向下）
  ├─ 等待 1000 毫秒
  └─ 获取新加载的数据
\`\`\`

---

### 真实鼠标滚动

使用系统级鼠标滚轮模拟，完全模拟用户真实的鼠标滚轮操作。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 滚动方向 | 滚动的方向 | 向上/向下 |
| 滚动次数 | 滚轮滚动的次数 | 3 |
| 每次滚动量 | 每次滚动的幅度 | 120（标准滚轮单位） |
| 滚动间隔 | 每次滚动之间的间隔（毫秒） | 100 |

**使用场景**：
- 网页有防滚动检测机制时
- 普通滚动方式无效时
- 需要最真实的滚动模拟时

**⚠️ 重要提示**：
- 使用此模块时，鼠标必须位于需要滚动的页面区域内
- 这是系统级操作，会真实移动鼠标滚轮
- 适用于任何网页，无法被网页检测或阻止

**与普通滚动的区别**：
| 特性 | 滚动页面 | 真实鼠标滚动 |
|------|----------|--------------|
| 实现方式 | 浏览器API | 系统级鼠标事件 |
| 防检测 | 可能被检测 | 无法被检测 |
| 鼠标位置 | 不需要 | 需要在目标区域 |
| 适用范围 | 普通网页 | 所有网页 |

---

## 📸 截图模块

### 网页截图

截取当前浏览器页面的图片。

**配置项**：

| 参数 | 说明 |
|------|------|
| 截图类型 | 整页截图/可视区域/指定元素 |
| 保存路径 | 图片保存位置（可选） |
| 保存到变量 | 将图片路径保存到变量 |

**截图类型说明**：
- **整页截图**：截取整个网页（包括滚动区域）
- **可视区域**：只截取当前可见部分
- **指定元素**：截取特定元素的图片

💡 如需截取整个屏幕（包括浏览器外的内容），请使用系统操作分类中的「屏幕截图」模块

---

### 保存图片

保存网页中的图片元素。

**配置项**：

| 参数 | 说明 |
|------|------|
| 选择器 | 图片元素的选择器 |
| 保存路径 | 图片保存位置 |
| 保存到变量 | 将图片保存到变量 |

**使用场景**：
- 保存商品图片
- 截取验证码图片用于识别

---

## 📊 数据处理模块

### 设置变量

创建或修改变量的值。

**配置项**：

| 参数 | 说明 |
|------|------|
| 变量名 | 变量的名称 |
| 变量值 | 要设置的值 |
| 变量类型 | 字符串/数字/布尔/列表/字典 |

---

### JSON解析

从JSON字符串中提取数据。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| JSON数据 | JSON字符串或变量 | \`{响应数据}\` |
| JSONPath | 提取路径 | \`$.data.name\` |
| 保存到变量 | 存储结果的变量名 | 用户名 |

---

### 随机数

生成随机数字。

**配置项**：

| 参数 | 说明 |
|------|------|
| 最小值 | 随机数的最小值 |
| 最大值 | 随机数的最大值 |
| 保存到变量 | 存储结果的变量名 |

---

### 获取时间

获取当前日期和时间。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 时间格式 | 输出的格式 | YYYY-MM-DD HH:mm:ss |
| 保存到变量 | 存储结果的变量名 | 当前时间 |

---

### 读取Excel

从Excel文件读取数据。

**配置项**：

| 参数 | 说明 |
|------|------|
| 文件 | 选择已上传的Excel文件资源 |
| 工作表 | 要读取的工作表名称 |
| 保存到变量 | 存储数据的变量名 |

**数据格式**：读取后的数据是一个列表，每行是一个字典。

---

## 📋 辅助工具模块

### 打印日志

输出调试信息到日志面板。

**配置项**：

| 参数 | 说明 |
|------|------|
| 日志内容 | 要输出的文本，支持变量引用 |
| 日志级别 | info/success/warning/error |

**调试示例**：
\`\`\`
当前处理第 {循环索引} 条数据
商品名称：{商品名称}
商品价格：{商品价格}
\`\`\`

---

### 播放提示音

播放系统提示音提醒。

**配置项**：

| 参数 | 说明 |
|------|------|
| 提示音次数 | 播放几次提示音 |
| 间隔时间 | 每次提示音之间的间隔（毫秒） |

**使用场景**：
- 任务完成提醒
- 需要人工干预时提醒
- 错误发生时警告

---

### 播放音乐

播放指定URL的音频文件。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 音频URL | 音频文件的网络地址 | \`https://example.com/music.mp3\` |
| 等待播放完成 | 是否等待音乐播放完毕 | 是/否 |

**支持格式**：MP3、WAV、OGG、FLAC、M4A、AAC、WebM 等常见音频格式

**使用场景**：
- 任务完成时播放提示音乐
- 重要事件发生时播放通知音
- 定时任务的声音提醒

**注意**：
- URL可以不带 \`https://\` 前缀，系统会自动补全
- 选择"否（后台播放）"时，音乐会在后台播放，工作流继续执行
- 停止工作流时会自动停止正在播放的音乐

---

### 用户输入

弹出对话框让用户输入内容。

**配置项**：

| 参数 | 说明 |
|------|------|
| 提示文字 | 显示给用户的提示 |
| 默认值 | 输入框的默认内容 |
| 保存到变量 | 存储用户输入的变量名 |

**使用场景**：
- 输入验证码
- 输入动态密码
- 确认操作

---

## 💻 脚本模块

### JS脚本

在浏览器环境中执行JavaScript代码，用于数据处理、逻辑判断等。

**配置项**：

| 参数 | 说明 |
|------|------|
| 脚本代码 | 要执行的JavaScript代码 |

**执行环境**：
- 运行在浏览器前端环境
- 可以访问标准JavaScript内置对象（Array、Object、Math、Date等）
- 可以通过 \`vars\` 对象读写工作流变量

**变量读写**：
\`\`\`javascript
// 读取变量
const userName = vars.用户名;
const count = vars.计数器;

// 修改变量（只能修改已存在的变量）
vars.计数器 = count + 1;
vars.结果 = "处理完成";

// 注意：不能创建新变量，只能修改已存在的变量
\`\`\`

**使用场景**：
- 数据处理和转换
- 数学计算
- 字符串处理
- 逻辑判断
- 数组和对象操作

**示例1：数据处理**
\`\`\`javascript
// 处理价格字符串
const priceStr = vars.价格; // "¥99.00"
const price = parseFloat(priceStr.replace(/[¥,]/g, ''));
vars.价格数值 = price;
\`\`\`

**示例2：数组操作**
\`\`\`javascript
// 过滤数据
const list = vars.商品列表;
const filtered = list.filter(item => item.price > 100);
vars.筛选结果 = filtered;
\`\`\`

**AI编码助手**：
- 点击代码编辑器右上角的"AI编码助手"按钮
- 输入需求描述，AI会自动生成代码
- AI会自动识别当前工作流中的变量
- 生成的代码可以直接插入到编辑器中

---

### Python脚本

在Python环境中执行Python代码，功能更强大。

**配置项**：

| 参数 | 说明 |
|------|------|
| 脚本代码 | 要执行的Python代码 |

**执行环境**：
- 运行在Python 3.13环境
- 可以使用常用的Python库（requests、pandas、numpy、pillow等）
- 可以通过 \`vars\` 字典读写工作流变量

**变量读写**：
\`\`\`python
# 读取变量
user_name = vars['用户名']
count = vars['计数器']

# 修改变量（只能修改已存在的变量）
vars['计数器'] = count + 1
vars['结果'] = "处理完成"

# 注意：不能创建新变量，只能修改已存在的变量
\`\`\`

**可用库**：
- requests：HTTP请求
- pandas：数据分析
- numpy：数值计算
- pillow：图像处理
- openpyxl：Excel操作
- json：JSON处理
- re：正则表达式
- datetime：日期时间
- pathlib：路径操作
- base64：编码解码

**使用场景**：
- 复杂的数据处理
- 文件操作
- 网络请求
- 图像处理
- 文本分析
- 系统操作

**示例1：文件操作**
\`\`\`python
from pathlib import Path

# 读取文件
file_path = vars['文件路径']
content = Path(file_path).read_text(encoding='utf-8')
vars['文件内容'] = content
\`\`\`

**示例2：数据处理**
\`\`\`python
import pandas as pd

# 处理Excel数据
data = vars['Excel数据']
df = pd.DataFrame(data)
result = df[df['价格'] > 100]
vars['筛选结果'] = result.to_dict('records')
\`\`\`

**AI编码助手**：
- 点击代码编辑器右上角的"AI编码助手"按钮
- 输入需求描述，AI会自动生成Python代码
- AI会自动识别当前工作流中的变量
- 生成的代码可以直接插入到编辑器中

---

### JS脚本注入

将JavaScript代码注入到当前网页中执行，可以操作网页DOM。

**配置项**：

| 参数 | 说明 |
|------|------|
| 脚本代码 | 要注入的JavaScript代码 |
| 保存返回值到变量 | 存储脚本返回值的变量名 |

**执行环境**：
- 运行在目标网页的浏览器环境中
- 可以访问网页的 document、window 等对象
- 可以操作网页DOM、调用网页的JavaScript函数
- 可以访问 localStorage、sessionStorage 等

**可用API**：
- document：操作DOM元素
- window：访问窗口对象
- console：输出日志
- localStorage/sessionStorage：本地存储
- fetch：发起网络请求
- 所有浏览器标准API

**使用场景**：
- 修改网页样式
- 提取网页数据
- 模拟用户操作
- 监听网页事件
- 自动填表
- 页面美化

**示例1：修改样式**
\`\`\`javascript
// 隐藏广告
document.querySelectorAll('.ad-banner').forEach(el => {
  el.style.display = 'none';
});
\`\`\`

**示例2：提取数据**
\`\`\`javascript
// 提取所有链接
const links = Array.from(document.querySelectorAll('a')).map(a => ({
  text: a.textContent,
  href: a.href
}));
return links; // 返回值会保存到变量
\`\`\`

**示例3：自动填表**
\`\`\`javascript
// 自动填写表单
document.querySelector('#username').value = '用户名';
document.querySelector('#password').value = '密码';
document.querySelector('#submit').click();
\`\`\`

**AI编码助手**：
- 点击代码编辑器右上角的"AI编码助手"按钮
- 输入需求描述，AI会自动生成网页操作代码
- AI会提供常见的网页操作示例
- 生成的代码可以直接插入到编辑器中

**注意事项**：
- 脚本在网页环境中执行，不能访问工作流变量
- 需要先打开网页才能注入脚本
- 某些网站可能有安全限制
- 返回值会自动序列化为JSON格式`
