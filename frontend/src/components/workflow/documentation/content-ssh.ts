export const sshGuideContent = `# 🔐 SSH 远程操作

本章介绍如何使用 WebRPA 通过 SSH 协议远程连接服务器，执行命令、上传下载文件，实现服务器运维自动化。

---

## 概述

SSH 模块共 **5 个**，基于 Paramiko 库实现，支持密码和密钥两种认证方式。

| 模块 | 说明 |
|------|------|
| ssh_connect | 建立 SSH 连接 |
| ssh_execute_command | 执行远程命令 |
| ssh_upload_file | 上传文件（SFTP）|
| ssh_download_file | 下载文件（SFTP）|
| ssh_disconnect | 断开连接 |

---

## 🔌 建立 SSH 连接（ssh_connect）

建立到远程服务器的 SSH 连接。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| 主机地址 | 服务器 IP 或域名 | \`192.168.1.100\` |
| 端口 | SSH 端口 | \`22\` |
| 用户名 | 登录用户名 | \`root\` |
| 认证方式 | 密码 / 私钥文件 | 密码 |
| 密码 | 登录密码 | \`{server_pwd}\` |
| 私钥路径 | PEM 私钥文件路径 | \`C:\\id_rsa\` |
| 超时（秒） | 连接超时时间 | \`30\` |
| 结果变量 | 保存 SSH 连接对象 | \`ssh_conn\` |

---

## ⚡ 执行远程命令（ssh_execute_command）

在远程服务器上执行 Shell 命令，并获取输出结果。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| SSH连接变量 | 已建立的连接 | \`{ssh_conn}\` |
| 命令 | 要执行的 Shell 命令 | \`ls -la /var/log\` |
| 超时（秒） | 命令执行超时 | \`60\` |
| 结果变量 | 保存命令输出（stdout） | \`cmd_output\` |
| 错误变量 | 保存错误输出（stderr） | \`cmd_error\` |
| 返回码变量 | 保存退出码 | \`exit_code\` |

**示例**（查看磁盘使用情况）：

\`\`\`
SSH执行命令 → 连接: {ssh}, 命令: df -h → 结果变量: disk_info
打印日志 → 内容: {disk_info}
\`\`\`

**执行多条命令**：用 \`&&\` 或 \`;\` 连接多条命令：

\`\`\`
cd /opt/myapp && git pull && systemctl restart myapp
\`\`\`

**执行需要 sudo 的命令**：

\`\`\`
echo '{sudo_password}' | sudo -S systemctl restart nginx
\`\`\`

---

## 📤 上传文件（ssh_upload_file）

通过 SFTP 将本地文件上传到远程服务器。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| SSH连接变量 | 已建立的连接 | \`{ssh_conn}\` |
| 本地路径 | 本地文件路径 | \`C:\\data\\report.csv\` |
| 远程路径 | 服务器目标路径 | \`/home/user/reports/\` |
| 结果变量 | 上传是否成功 | \`upload_ok\` |

---

## 📥 下载文件（ssh_download_file）

通过 SFTP 从远程服务器下载文件到本地。

**配置项**：

| 参数 | 说明 | 示例 |
|------|------|------|
| SSH连接变量 | 已建立的连接 | \`{ssh_conn}\` |
| 远程路径 | 服务器文件路径 | \`/var/log/app.log\` |
| 本地路径 | 本地保存路径 | \`C:\\logs\\app.log\` |
| 结果变量 | 下载是否成功 | \`download_ok\` |

---

## 🔌 断开连接（ssh_disconnect）

关闭 SSH 连接，释放资源。

| 参数 | 说明 |
|------|------|
| SSH连接变量 | 要关闭的连接 |

> **重要**：务必在工作流结束时断开 SSH 连接，避免连接泄漏。

---

## 📋 完整示例：自动化服务器部署

\`\`\`mermaid
flowchart TD
    A[SSH连接服务器] --> B[上传新版本包]
    B --> C[执行解压命令]
    C --> D[执行部署脚本]
    D --> E[检查服务状态]
    E --> F{服务正常?}
    F --是--> G[发送成功通知]
    F --否--> H[发送失败告警]
    G --> I[断开SSH连接]
    H --> I
\`\`\`

---

## 💡 使用技巧

- **连接复用**：一次连接后可执行多条命令，最后再断开
- **变量引用**：命令中可用 \`{变量名}\` 引用工作流变量
- **错误处理**：检查返回码变量（\`0\` 表示成功），非零则说明命令失败
- **私钥认证**：生产环境推荐使用私钥认证，更安全
- **全局配置**：在「全局配置」→「SSH」中预设服务器信息
`
